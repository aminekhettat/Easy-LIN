[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [
    ".git",
    ".pytest_cache",
    ".ruff_cache",
    ".venv",
    "docs/_build",
    "LDF",
]

[tool.ruff.lint]
select = ["E", "F", "B"]
ignore = ["E501"]
